# MongoDB connection string (required for auth + activity storage)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/fairsight?retryWrites=true&w=majority

# JWT signing secret — change this to a long random string in production
JWT_SECRET=change-me-to-a-random-secret-at-least-32-chars

# Google Gemini API key (optional — users can supply their own in the UI)
GOOGLE_AI_API_KEY=AIzaSyB779NF6ODn1THNpVUUopcasvJZQghhdZY
